<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RandomFromExel.MainPage">

    <StackLayout Padding="10">
        <!-- 1-qator: TextBox -->
        <Entry x:Name="inputEntry" Placeholder="Matn kiriting" />

        <Button 
            Text="Kiritish" 
            Clicked="Kirit" 
            WidthRequest="200"
            Margin="20"
            Padding="10"
            />

        <Button 
            Text="Tanlash" 
            Clicked="Tanla" 
            WidthRequest="200"
            Margin="20"
            Padding="10"
            />
        <Label 
            x:Name="resultLabel" 
            Text="Natija" 
            HorizontalOptions="Center"
            FontSize="24"/>
        
    </StackLayout>

</ContentPage>
